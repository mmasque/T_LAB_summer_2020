function emission_matrix = get_eM_emission_matrix(dotfname, dims)
    % gets the emitted bits for the transitions in the matrix. 
    % results_fname - the results file name produced by CSSR
    emission_matrix = NaN(dims);
    
end